{
  "name": "dispatch-e2e",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "playwright test",
    "humo": "playwright test --project=humo",
    "interfaz": "playwright test --project=interfaz"
  },
  "devDependencies": {
    "@playwright/test": "^1.63.0",
    "dotenv": "^18.0.1"
  }
}
