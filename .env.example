TEST_USERNAME=candidate
TEST_PASSWORD=replace_me
BACKEND_PORT=3000
FRONTEND_PORT=8080

# URL de la aplicacion web que usan las suites de interfaz y humo.
# Opcional: si no se define, se asume http://localhost:8080 (Docker Compose).
# Con el servidor de desarrollo de Vite seria http://localhost:5173
BASE_URL=http://localhost:8080

# URL de la API que usa la suite de humo.
# Opcional: si no se define, se asume http://localhost:3000
API_URL=http://localhost:3000
